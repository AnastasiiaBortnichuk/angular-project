import { UploadDirective } from './app-upload.directive';

describe('AppUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadDirective();
    expect(directive).toBeTruthy();
  });
});
